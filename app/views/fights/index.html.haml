%h1 Listing fights

%table
  %thead
    %tr
      %th User
      %th User won
      %th Superhero
      %th Ended at
      %th
      %th
      %th

  %tbody
    - @fights.each do |fight|
      %tr
        %td= fight.user
        %td= fight.user_won
        %td= fight.superhero_id
        %td= fight.ended_at
        %td= link_to 'Show', fight
        %td= link_to 'Edit', edit_fight_path(fight)
        %td= link_to 'Destroy', fight, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Fight', new_fight_path
